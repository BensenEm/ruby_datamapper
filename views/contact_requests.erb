<p>
  <table>
  <tr>
    <td>ID</td>
    <td>Name</td>
    <td>Email</td>
    <td>Nachricht</td>
    <td>Aktion</td>
  </tr>
  <% @all_contacts.each do |record| %>
    <tr>
      <td><%= record.id %></td>
      <td><%= record.name %></td>
      <td><%= record.email %></td>
      <td><%= record.message %></td>
      <td><a href="/admin/contact-requests/<%= record.id %>.html">Anzeigen</a><br>
          <a href="/admin/contact-requests/<%= record.id %>/edit.html">Bearbeiten</<a><br>
          <a href="/admin/contact-requests/<%= record.id %>/edit.html">Löschen</<a>
      </td>
    </tr>
  <% end %>
</table>
</p>
